import React from "react";

class Loading extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return ( <h1> 로딩 중.. </h1> );
    }
}

export default Loading;